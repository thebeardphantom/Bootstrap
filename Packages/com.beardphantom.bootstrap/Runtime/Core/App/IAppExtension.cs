namespace BeardPhantom.Bootstrap
{
    /// <summary>
    /// Marker interface for app extension types registered via <see cref="AppExtensionTypeAttribute"/> and
    /// retrieved through <see cref="App.GetExtension{T}"/>.
    /// </summary>
    public interface IAppExtension { }
}